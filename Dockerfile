# Встановлюємо образи
FROM node:20.11.1

# Вказуємо папку застосунку
WORKDIR /app

#  Копіюємо файли з корня каталогу "." в корень каталогу "/app"
COPY . .

# Запускаємо установку пакетів 
RUN npm install 

# Вказуємо порт, який буде слухати контейнер
EXPOSE 3000

# Запускаємо програму
CMD ["node", "app"]       


# docker build .  створеня зборки образу
# docker run "id образу"  запуск образу
